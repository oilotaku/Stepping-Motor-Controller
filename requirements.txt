# DS102/DS112 步進馬達控制器 — 相依套件
# 安裝： venv\Scripts\python.exe -m pip install -r requirements.txt
# （tkinter 為 Python 3 內建，不需列於此）

pyserial==3.5       # RS-232/USB 序列通訊，main.py / main_ai.py / probe_ds102.py
PyVISA==1.16.2      # GPIB 量測儀器控制，meter_GPIB.py
requests==2.34.2    # HTTP 用戶端（間接/選用）
matplotlib==3.11.1  # 尋光即時軌跡圖，main_ai.py 尋光分頁（fiber_scanner 整合）
numpy==2.5.2         # matplotlib 的必要相依，main_ai.py 也直接用來組空陣列（set_offsets 需要）
XlsxWriter==3.2.9   # 尋光樣本的 Excel 報表輸出，fiber_scanner.export_samples_xlsx（缺席時優雅降級，只影響 xlsx，不影響 JSON 樣本檔）

# --- 開發/測試用相依（不影響 GUI 執行期）---
pytest==9.1.1        # verify_scan_tab.py / verify_meter_panel.py 的測試框架，VS Code Test Explorer 也靠它發現測試
